---
description: Kandev PR fixup workflow prompt for Cursor agents.
globs: "scripts/pr-state, scripts/pr-resolve, .agents/skills/pr-fixup/**"
alwaysApply: false
---

# Kandev PR Fixup

Use `.agents/skills/pr-fixup/SKILL.md` for the workflow. In a user-started
primary session, handle polling and remediation directly in the same
conversation; rerun only the affected task-defined check after a fix.

If GitHub access requires approval, surface that gate
to the user and stop. Do not relaunch polling after approval is denied,
cancelled, or interrupted; the shared skill's bounded retry behavior applies
only to actual fetch failures after access was approved. Its helper scripts are
at `<worktree>/scripts/...`, not under the skill directory.
