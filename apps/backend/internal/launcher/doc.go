// Package launcher implements the native kandev terminal entrypoint.
//
// Exit codes follow the usual CLI convention: 0 means success, 2 means usage
// or parse errors, and 1 means runtime failures after arguments are accepted.
package launcher
