#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
export HOME="${KANDEV_REPLIT_HOME:-$ROOT_DIR/.kandev-home}"
mkdir -p "$HOME"

exec npx --yes opencode-ai@1.18.31 "$@"